<div>customerチャットルームの一覧</div>
<% @reservations.each do |reservation| %>
  <div>
    <%= reservation.user.nickname %>
    <%= reservation.day %>
    <%= link_to "DM", room_path(reservation) %>
  </div>
<% end %>